#include<stdio.h>
int main()
{
int n,a[100],i,sum=0;
printf("enter the size of array:");
scanf("%d",&n);
printf("\n enter the elements in array:");
for(i=0;i<n;i++)
{
scanf("%d",&a[i]);
}
for(i=0;i<n;i++)
{
sum=sum+a[i];
}
printf("\n sum of elements is %d",sum);
return 0;
}
